/*  BMI Calculator Challenge: Create a BMI calculator using JavaScript functions.
create a function that takes weight and height as inputs and gives the calculated 
BMI value as an output. The output should be rounded to the nearest whole number.

The first parameter should be the weight and the second should be the height.
 */ 

function bmiCalculator(weight, height)
{
	var = Math.round(weight / (height*height));
	return(bmi);
}
